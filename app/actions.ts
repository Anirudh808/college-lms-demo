"use server";

// Server-side session check - for demo we rely on client Zustand
// This is a placeholder; in real app you'd use cookies/session
export async function getServerSession() {
  return null;
}
